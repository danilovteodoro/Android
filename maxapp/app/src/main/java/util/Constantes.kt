package util

class Constantes {
    companion object {
        const val BASE_URL = "https://api.myjson.com/bins/"
        const val DATE_PATTERN="yyyy-MM-dd'T'HH:mm:ss.SSS"
        const val DATETIME_SQLITE="yyyy-MM-dd HH:mm:ss.SSS"
        const val BR_DATE_PATTERN="dd/MM/yyyy HH:mm"

    }
}